# Admin dashboard (Vite) environment template.
# Copy to .env and fill in every value — all variables are required.

# Base URL of the public landing page (Next.js app). Used to build
# per-branch preview links, e.g. http://barishal.localhost:3001.
# Required.
VITE_LANDING_URL=
